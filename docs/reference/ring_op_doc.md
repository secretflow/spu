<!-- Autogenerated by mlir-tblgen; don't manually edit -->
### `ring.arshift` (spu::ring::ARShiftOp)

Syntax:

```
operation ::= `ring.arshift` $lhs `,` $rhs attr-dict
              `:` custom<SameOperandsAndResultType>(type($lhs), type($rhs), type($result))
```


Traits: `AlwaysSpeculatableImplTrait`, `Elementwise`, `Ring_SameOperandsAndResultsSemanticRingType`, `SameOperandsAndResultShape`

Interfaces: `ConditionallySpeculatable`, `InferTypeOpInterface`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `lhs` | any-secret-container
| `rhs` | container with value semantics of signless integer or unsigned integer values

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | any-secret-container

### `ring.add` (spu::ring::AddOp)

Syntax:

```
operation ::= `ring.add` $lhs `,` $rhs attr-dict
              `:` custom<SameOperandsAndResultType>(type($lhs), type($rhs), type($result))
```


Traits: `AlwaysSpeculatableImplTrait`, `Commutative`, `Elementwise`, `Ring_SameOperandsAndResultsSemanticRingType`, `SameOperandsAndResultShape`

Interfaces: `ConditionallySpeculatable`, `InferShapedTypeOpInterface`, `InferTypeOpInterface`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `lhs` | any-ring-like-container
| `rhs` | any-ring-like-container

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | any-secret-container

### `ring.and` (spu::ring::AndOp)

Syntax:

```
operation ::= `ring.and` $lhs `,` $rhs attr-dict
              `:` custom<SameOperandsAndResultType>(type($lhs), type($rhs), type($result))
```


Traits: `AlwaysSpeculatableImplTrait`, `Commutative`, `Elementwise`, `Ring_SameOperandsAndResultsSemanticRingType`, `SameOperandsAndResultShape`

Interfaces: `ConditionallySpeculatable`, `InferShapedTypeOpInterface`, `InferTypeOpInterface`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `lhs` | any-ring-like-container
| `rhs` | any-ring-like-container

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | any-secret-container

### `ring.bitrev` (spu::ring::BitRevOp)

Syntax:

```
operation ::= `ring.bitrev` $operand attr-dict `:` custom<SameOperandsAndResultType>(type($operand), type($result))
```


Traits: `AlwaysSpeculatableImplTrait`, `Elementwise`, `SameOperandsAndResultType`

Interfaces: `ConditionallySpeculatable`, `InferTypeOpInterface`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>start</code></td><td>::mlir::IntegerAttr</td><td>64-bit signless integer attribute</td></tr>
<tr><td><code>end</code></td><td>::mlir::IntegerAttr</td><td>64-bit signless integer attribute</td></tr>
</table>

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `operand` | any-secret-container

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | any-secret-container

### `ring.broadcast_as` (spu::ring::BroadcastShapeAsOp)

Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `lhs` | any type
| `rhs` | any type

#### Results:

| Result | Description |
| :----: | ----------- |
&laquo;unnamed&raquo; | any type

### `ring.cast` (spu::ring::CastOp)

Syntax:

```
operation ::= `ring.cast` $operand attr-dict `:` custom<SameOperandsAndResultType>(type($operand), type($result))
```


Traits: `AlwaysSpeculatableImplTrait`, `Elementwise`, `SameOperandsAndResultShape`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `operand` | any-secret-container

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | any-secret-container

### `ring.custom_call` (spu::ring::CustomCallOp)

Syntax:

```
operation ::= `ring.custom_call` custom<CustomCallTarget>($call_target_name) `(` $inputs `)`
              attr-dict `:` functional-type(operands, results)
```


Interfaces: `MemoryEffectOpInterface`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>call_target_name</code></td><td>::mlir::StringAttr</td><td>string attribute</td></tr>
<tr><td><code>has_side_effect</code></td><td>::mlir::BoolAttr</td><td>bool attribute</td></tr>
</table>

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `inputs` | variadic of any type

#### Results:

| Result | Description |
| :----: | ----------- |
&laquo;unnamed&raquo; | variadic of any type

### `ring.decode_from_fxp` (spu::ring::DecodeFromFxpOp)

Syntax:

```
operation ::= `ring.decode_from_fxp` $operand `,` `fxp_bits` `=` $fxp_bits attr-dict `:` functional-type(operands, results)
```


Traits: `AlwaysSpeculatableImplTrait`, `Elementwise`, `SameOperandsAndResultShape`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>fxp_bits</code></td><td>::mlir::IntegerAttr</td><td>64-bit signless integer attribute</td></tr>
</table>

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `operand` | container with value semantics of signless integer values

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | container with value semantics of floating-point values

### `ring.dot` (spu::ring::DotOp)

Syntax:

```
operation ::= `ring.dot` $lhs `,` $rhs attr-dict `:` functional-type(operands, results)
```


Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `lhs` | any-ring-like-container
| `rhs` | any-ring-like-container

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | any-secret-container

### `ring.encode_to_fxp` (spu::ring::EncodeToFxpOp)

Syntax:

```
operation ::= `ring.encode_to_fxp` $operand `,` `fxp_bits` `=` $fxp_bits attr-dict `:` functional-type(operands, results)
```


Traits: `AlwaysSpeculatableImplTrait`, `Elementwise`, `SameOperandsAndResultShape`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>fxp_bits</code></td><td>::mlir::IntegerAttr</td><td>64-bit signless integer attribute</td></tr>
</table>

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `operand` | container with value semantics of floating-point values

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | container with value semantics of signless integer values

### `ring.equal` (spu::ring::EqualOp)

Syntax:

```
operation ::= `ring.equal` $lhs `,` $rhs attr-dict `:` functional-type(operands, results)
```


Traits: `AlwaysSpeculatableImplTrait`, `Commutative`, `Elementwise`, `Ring_SameOperandsSemanticRingType`, `SameOperandsAndResultShape`

Interfaces: `ConditionallySpeculatable`, `InferShapedTypeOpInterface`, `InferTypeOpInterface`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `lhs` | any-ring-like-container
| `rhs` | any-ring-like-container

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | container with value semantics of Secret of secret boolean values values

### `ring.lshift` (spu::ring::LShiftOp)

Syntax:

```
operation ::= `ring.lshift` $lhs `,` $rhs attr-dict
              `:` custom<SameOperandsAndResultType>(type($lhs), type($rhs), type($result))
```


Traits: `AlwaysSpeculatableImplTrait`, `Elementwise`, `Ring_SameOperandsAndResultsSemanticRingType`, `SameOperandsAndResultShape`

Interfaces: `ConditionallySpeculatable`, `InferTypeOpInterface`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `lhs` | any-secret-container
| `rhs` | container with value semantics of signless integer or unsigned integer values

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | any-secret-container

### `ring.less` (spu::ring::LessOp)

Syntax:

```
operation ::= `ring.less` $lhs `,` $rhs attr-dict `:` functional-type(operands, results)
```


Traits: `AlwaysSpeculatableImplTrait`, `Elementwise`, `Ring_SameOperandsSemanticRingType`, `SameOperandsAndResultShape`

Interfaces: `ConditionallySpeculatable`, `InferShapedTypeOpInterface`, `InferTypeOpInterface`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `lhs` | any-ring-like-container
| `rhs` | any-ring-like-container

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | container with value semantics of Secret of secret boolean values values

### `ring.msb` (spu::ring::MsbOp)

Syntax:

```
operation ::= `ring.msb` $operand attr-dict `:` custom<SameOperandsAndResultType>(type($operand), type($result))
```


Traits: `AlwaysSpeculatableImplTrait`, `Elementwise`, `SameOperandsAndResultShape`

Interfaces: `ConditionallySpeculatable`, `InferShapedTypeOpInterface`, `InferTypeOpInterface`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `operand` | any-secret-container

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | container with value semantics of Secret of secret boolean values values

### `ring.mul` (spu::ring::MulOp)

Syntax:

```
operation ::= `ring.mul` $lhs `,` $rhs attr-dict
              `:` custom<SameOperandsAndResultType>(type($lhs), type($rhs), type($result))
```


Traits: `AlwaysSpeculatableImplTrait`, `Commutative`, `Elementwise`, `SameOperandsAndResultShape`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `lhs` | any-ring-like-container
| `rhs` | any-ring-like-container

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | any-secret-container

### `ring.neg` (spu::ring::NegOp)

Syntax:

```
operation ::= `ring.neg` $operand attr-dict `:` custom<SameOperandsAndResultType>(type($operand), type($result))
```


Traits: `AlwaysSpeculatableImplTrait`, `Elementwise`, `SameOperandsAndResultType`

Interfaces: `ConditionallySpeculatable`, `InferTypeOpInterface`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `operand` | any-secret-container

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | any-secret-container

### `ring.not` (spu::ring::NotOp)

Syntax:

```
operation ::= `ring.not` $operand attr-dict `:` custom<SameOperandsAndResultType>(type($operand), type($result))
```


Traits: `AlwaysSpeculatableImplTrait`, `Elementwise`, `SameOperandsAndResultType`

Interfaces: `ConditionallySpeculatable`, `InferTypeOpInterface`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `operand` | any-secret-integer-container

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | any-secret-integer-container

### `ring.p2s` (spu::ring::P2SOp)

Syntax:

```
operation ::= `ring.p2s` $operand attr-dict `:` custom<SameOperandsAndResultType>(type($operand), type($result))
```


Traits: `AlwaysSpeculatableImplTrait`, `Elementwise`, `Ring_SameOperandsAndResultsSemanticRingType`, `SameOperandsAndResultShape`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `operand` | container with value semantics of signless integer or unsigned integer values

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | any-secret-container

### `ring.rshift` (spu::ring::RShiftOP)

Syntax:

```
operation ::= `ring.rshift` $lhs `,` $rhs attr-dict
              `:` custom<SameOperandsAndResultType>(type($lhs), type($rhs), type($result))
```


Traits: `AlwaysSpeculatableImplTrait`, `Elementwise`, `Ring_SameOperandsAndResultsSemanticRingType`, `SameOperandsAndResultShape`

Interfaces: `ConditionallySpeculatable`, `InferTypeOpInterface`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `lhs` | any-secret-container
| `rhs` | container with value semantics of signless integer or unsigned integer values

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | any-secret-container

### `ring.reduce_window` (spu::ring::ReduceWindowOp)

Traits: `RecursiveMemoryEffects`, `SameVariadicOperandSize`, `SingleBlockImplicitTerminator<ReturnOp>`, `SingleBlock`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>window_dimensions</code></td><td>::mlir::DenseI64ArrayAttr</td><td>i64 dense array attribute</td></tr>
<tr><td><code>window_strides</code></td><td>::mlir::DenseI64ArrayAttr</td><td>i64 dense array attribute</td></tr>
<tr><td><code>window_dilations</code></td><td>::mlir::DenseI64ArrayAttr</td><td>i64 dense array attribute</td></tr>
</table>

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `inputs` | variadic of any-ring-like-container
| `init_values` | variadic of any-ring-like-container

#### Results:

| Result | Description |
| :----: | ----------- |
&laquo;unnamed&raquo; | variadic of any-ring-like-container

### `ring.return` (spu::ring::ReturnOp)

Syntax:

```
operation ::= `ring.return` $results attr-dict (`:` type($results)^)?
```


Traits: `AlwaysSpeculatableImplTrait`, `ReturnLike`, `Terminator`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`, `RegionBranchTerminatorOpInterface`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `results` | variadic of any-ring-like-container

### `ring.reverse` (spu::ring::ReverseOp)

Syntax:

```
operation ::= `ring.reverse` $operand `,` `dims` `=` $dimensions attr-dict `:` type($result)
```


Traits: `AlwaysSpeculatableImplTrait`, `SameOperandsAndResultType`

Interfaces: `ConditionallySpeculatable`, `InferTypeOpInterface`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>dimensions</code></td><td>::mlir::DenseI64ArrayAttr</td><td>i64 dense array attribute</td></tr>
</table>

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `operand` | shaped of any type values

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | shaped of any type values

### `ring.secret_extract_slice` (spu::ring::SecretExtractSliceOp)

Syntax:

```
operation ::= `ring.secret_extract_slice` operands `indexing_dim` `=` $indexing_dim
              attr-dict `:` functional-type(operands, results)
```


Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>indexing_dim</code></td><td>::mlir::DenseI64ArrayAttr</td><td>i64 dense array attribute</td></tr>
</table>

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `operand` | any-ring-like-container
| `indices` | variadic of any-secret-integer-container

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | any-secret-container

### `ring.secret_insert_slice` (spu::ring::SecretInsertSliceOp)

Syntax:

```
operation ::= `ring.secret_insert_slice` operands attr-dict `:` functional-type(operands, results)
```


Traits: `AlwaysSpeculatableImplTrait`

Interfaces: `ConditionallySpeculatable`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>prefer_in_place</code></td><td>::mlir::BoolAttr</td><td>bool attribute</td></tr>
</table>

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `operand` | any-ring-like-container
| `update` | any-ring-like-container
| `start_indices` | variadic of scalar integer like

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | any-secret-container

### `ring.sort` (spu::ring::SortOp)

Traits: `RecursiveMemoryEffects`, `SameOperandsAndResultShape`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>dimension</code></td><td>::mlir::IntegerAttr</td><td>64-bit signless integer attribute</td></tr>
<tr><td><code>is_stable</code></td><td>::mlir::BoolAttr</td><td>bool attribute</td></tr>
</table>

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `operands` | variadic of any-ring-like-container

#### Results:

| Result | Description |
| :----: | ----------- |
&laquo;unnamed&raquo; | variadic of any-ring-like-container

### `ring.trunc` (spu::ring::TruncOp)

Syntax:

```
operation ::= `ring.trunc` $operand attr-dict `:` custom<SameOperandsAndResultType>(type($operand), type($result))
```


Traits: `AlwaysSpeculatableImplTrait`, `Elementwise`, `SameOperandsAndResultType`

Interfaces: `ConditionallySpeculatable`, `InferTypeOpInterface`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>bits</code></td><td>::mlir::IntegerAttr</td><td>64-bit signless integer attribute</td></tr>
<tr><td><code>known_positive</code></td><td>::mlir::BoolAttr</td><td>bool attribute</td></tr>
</table>

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `operand` | any-secret-container

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | any-secret-container

### `ring.xor` (spu::ring::XorOp)

Syntax:

```
operation ::= `ring.xor` $lhs `,` $rhs attr-dict
              `:` custom<SameOperandsAndResultType>(type($lhs), type($rhs), type($result))
```


Traits: `AlwaysSpeculatableImplTrait`, `Commutative`, `Elementwise`, `Ring_SameOperandsAndResultsSemanticRingType`, `SameOperandsAndResultShape`

Interfaces: `ConditionallySpeculatable`, `InferShapedTypeOpInterface`, `InferTypeOpInterface`, `NoMemoryEffect (MemoryEffectOpInterface)`

Effects: `MemoryEffects::Effect{}`

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `lhs` | any-ring-like-container
| `rhs` | any-ring-like-container

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | any-secret-container

